# Database URL for libSQL/SQLite
# For local development, use a file-based database:
DATABASE_URL=file:./local.db

# For production with Turso or other libSQL providers:
# DATABASE_URL=libsql://your-database.turso.io
# DATABASE_AUTH_TOKEN=your-auth-token

# Optional: Secret token to protect the sync endpoint
# Uncomment the authentication code in src/routes/api/sync/+server.ts to use this
# SYNC_SECRET=your-secret-token-here
